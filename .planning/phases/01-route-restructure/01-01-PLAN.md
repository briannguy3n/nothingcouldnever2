---
phase: 01-route-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/personal.md
  - index.html
  - pages/about.md
  - pages/sets.md
autonomous: false
requirements:
  - PRSV-01
  - PRSV-02

must_haves:
  truths:
    - "Visiting /personal loads the existing landing page with magnetic link effects, logo rotation, footer flash, and under-construction tooltip all working"
    - "Blog, gallery, notes, about, contact, and sets pages load at their existing URLs without errors"
    - "Root / no longer serves the old landing page (returns 404 — old content moved)"
    - "Back-links on about and sets pages navigate to /personal instead of /"
  artifacts:
    - path: "pages/personal.md"
      provides: "Landing page at /personal URL"
      contains: "permalink: /personal"
    - path: "pages/about.md"
      provides: "About page with updated back-link"
      contains: 'href="/personal"'
    - path: "pages/sets.md"
      provides: "Sets page with updated back-link"
      contains: 'href="/personal"'
  key_links:
    - from: "pages/personal.md"
      to: "_layouts/landing.html"
      via: "layout: landing front matter"
      pattern: "layout: landing"
    - from: "_layouts/landing.html"
      to: "/assets/js/landing-effects.js"
      via: "script tag with absolute path"
      pattern: 'src="/assets/js/landing-effects.js"'
    - from: "pages/about.md"
      to: "/personal"
      via: "anchor href"
      pattern: 'href="/personal"'
    - from: "pages/sets.md"
      to: "/personal"
      via: "anchor href"
      pattern: 'href="/personal"'
---

<objective>
Move the existing landing page from `/` to `/personal` and update internal back-links so all existing site functionality is preserved at new URLs.

Purpose: Clear the root URL for the Phase 2 chooser splash while keeping everything that exists today fully functional. This is the safety-first foundation — nothing breaks before new pages are added.

Output: `pages/personal.md` created, `index.html` deleted, back-links in `about.md` and `sets.md` updated to `/personal`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-route-restructure/01-RESEARCH.md
@.planning/phases/01-route-restructure/01-PATTERNS.md
@.planning/phases/01-route-restructure/01-UI-SPEC.md

<interfaces>
<!-- Key files the executor needs to understand. Extracted from codebase. -->

From index.html (WILL BE DELETED — current state for reference):
```yaml
---
layout: landing
title: Home
---
```

From _layouts/landing.html (READ ONLY — not modified in this plan):
```html
<!doctype html>
<html>
  {% include head.html %}
  <body class="landing-page">
    <img src="/assets/img/pages/nothing_changes_logo.png" alt="Nothing Changes Logo" class="landing-logo">
    <div class="landing-main-links-container">
      <a href="/about" class="landing-link-about">about</a>
      <a href="/sets" class="landing-link-sets">sets</a>
      <a href="/" class="landing-link-music">music</a>
      <a href="/photos" class="landing-link-photos">photos</a>
      <a href="/notes-password" class="landing-link-notes">notes</a>
    </div>
    <!-- Under Construction Tooltip -->
    <div id="construction-tooltip" class="construction-tooltip">
      <span class="construction-close">&times;</span>
      <p>Under Construction</p>
      <small>This page is currently being built. Check back soon.</small>
    </div>
    {% include footer.html %}
    <script src="/assets/js/landing-effects.js"></script>
  </body>
</html>
```

From pages/about.md line 9 (the line to change):
```html
<a href="/" class="image-link" data-tooltip="Go back to home page">
```

From pages/sets.md line 12 (the line to change):
```html
<a href="/" class="sets-page-home-link">
```

From pages/about.md (full front matter pattern to match):
```yaml
---
layout: no-header
title: About
permalink: /about
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /personal page and remove root index.html</name>
  <files>pages/personal.md, index.html</files>
  <read_first>
    - index.html (current root — see its layout and title values before deletion)
    - _layouts/landing.html (the layout personal.md will use — verify all asset paths are absolute)
    - pages/about.md (front matter pattern to match: layout, title, permalink structure)
  </read_first>
  <action>
1. Create `pages/personal.md` with this exact content (complete file, no body):
```yaml
---
layout: landing
title: Personal
permalink: /personal
---
```
This follows the identical front matter pattern as `pages/about.md` and `pages/photos.md`. The `landing` layout builds the full page from its own HTML — no body content needed. The `permalink: /personal` directive tells Jekyll to serve this page at `/personal`.

2. Delete `index.html` from the repository root. Use `git rm index.html` so the deletion is tracked. After deletion, Jekyll has no root page — GitHub Pages will serve a 404 for `/`. This is expected and explicitly within spec (ROADMAP success criterion 3: "Root / no longer serves the old landing page — returns 404 or placeholder").

Do NOT modify `_layouts/landing.html`. Do NOT modify any other file in `pages/`. Do NOT add a replacement `index.html`. Do NOT touch `_config.yml`.
  </action>
  <verify>
    <automated>cd /Users/brian/Documents/GitHub/nothingcouldnever2 && test -f pages/personal.md && ! test -f index.html && grep -q "permalink: /personal" pages/personal.md && grep -q "layout: landing" pages/personal.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `pages/personal.md` exists and contains exactly three front matter keys: `layout: landing`, `title: Personal`, `permalink: /personal`
    - `pages/personal.md` has no body content after the closing `---`
    - `index.html` does not exist in the repository root
    - `_layouts/landing.html` is unmodified (no diff)
    - `_config.yml` is unmodified (no diff)
  </acceptance_criteria>
  <done>pages/personal.md exists with correct front matter. Root index.html is deleted. No other files changed.</done>
</task>

<task type="auto">
  <name>Task 2: Update back-links in about.md and sets.md to point to /personal</name>
  <files>pages/about.md, pages/sets.md</files>
  <read_first>
    - pages/about.md (see current href="/" on line 9 — the exact line to change)
    - pages/sets.md (see current href="/" on line 12 — the exact line to change)
  </read_first>
  <action>
1. In `pages/about.md`, change line 9 from:
```html
<a href="/" class="image-link" data-tooltip="Go back to home page">
```
to:
```html
<a href="/personal" class="image-link" data-tooltip="Go back to home page">
```
Only the `href` value changes from `"/"` to `"/personal"`. The class attribute, data-tooltip attribute, and all surrounding HTML must remain identical.

2. In `pages/sets.md`, change line 12 from:
```html
<a href="/" class="sets-page-home-link">
```
to:
```html
<a href="/personal" class="sets-page-home-link">
```
Only the `href` value changes from `"/"` to `"/personal"`. The class attribute and surrounding HTML must remain identical.

These updates ensure PRSV-02 compliance: clicking "back to home" on the about page and "Go back home" on the sets page navigates to `/personal` (the relocated landing page) instead of `/` (which now 404s).

Do NOT change any other lines in either file. Do NOT change the `data-tooltip` text. Do NOT change `permalink:` values in front matter. Do NOT modify any other file in `pages/`.
  </action>
  <verify>
    <automated>cd /Users/brian/Documents/GitHub/nothingcouldnever2 && grep -q 'href="/personal"' pages/about.md && grep -q 'href="/personal"' pages/sets.md && ! grep 'href="/"' pages/about.md && ! grep 'href="/"' pages/sets.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `pages/about.md` line 9 contains `href="/personal"` (not `href="/"`)
    - `pages/about.md` line 9 still contains `class="image-link"` and `data-tooltip="Go back to home page"`
    - `pages/sets.md` line 12 contains `href="/personal"` (not `href="/"`)
    - `pages/sets.md` line 12 still contains `class="sets-page-home-link"`
    - No other lines in either file are changed (diff shows exactly 1 line changed per file)
    - `permalink: /about` in about.md front matter is unchanged
    - `permalink: /sets` in sets.md front matter is unchanged
  </acceptance_criteria>
  <done>Both back-links updated to /personal. No other content changed in either file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Jekyll build and page functionality</name>
  <files>pages/personal.md, pages/about.md, pages/sets.md</files>
  <action>
Run `bundle exec jekyll build` to confirm the site compiles without errors. Then run `bundle exec jekyll serve` so the user can verify all pages visually.
  </action>
  <what-built>
    Moved landing page from / to /personal. Updated back-links in about and sets pages. Deleted root index.html.
  </what-built>
  <how-to-verify>
    1. Run `bundle exec jekyll serve` from the project root
    2. Visit `http://localhost:4000/personal` — confirm the landing page loads with:
       - Logo image visible and rotating on mouse movement
       - Navigation links (about, sets, music, photos, notes) present with magnetic repulsion effect
       - Footer visible with social icons
       - Clicking "music" shows the under-construction tooltip
    3. Visit `http://localhost:4000/` — confirm it returns a 404 (not the old landing page)
    4. Visit `http://localhost:4000/about` — confirm the about page loads and the profile image links to `/personal` (click the image, should navigate to the landing page at /personal)
    5. Visit `http://localhost:4000/sets` — confirm the sets page loads and "Go back home" links to `/personal`
    6. Visit `http://localhost:4000/photos` — confirm gallery loads normally
    7. Visit `http://localhost:4000/notes-password` — confirm password page loads normally
    8. Visit `http://localhost:4000/contact` — confirm contact page loads normally
  </how-to-verify>
  <verify>
    <automated>cd /Users/brian/Documents/GitHub/nothingcouldnever2 && bundle exec jekyll build 2>&1 | tail -5 && test -f _site/personal/index.html && ! test -f _site/index.html && echo "BUILD PASS" || echo "BUILD FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `bundle exec jekyll build` exits 0 with no errors
    - `_site/personal/index.html` exists in build output
    - `_site/index.html` does NOT exist in build output
    - User confirms landing page at /personal is fully functional
  </acceptance_criteria>
  <done>Jekyll builds clean. User has verified all pages work at their expected URLs.</done>
  <resume-signal>Type "approved" if all pages work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static file serving | GitHub Pages serves pre-built HTML; no dynamic input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Root URL 404 page | accept | Default GitHub Pages 404 reveals no sensitive info. Custom 404.md already exists in codebase with generic "page not found" message. No credentials, paths, or internal details exposed. |
| T-01-02 | Denial of Service | Landing page at new URL | accept | Static site — no amplification vector. GitHub Pages CDN handles availability. No change in attack surface from URL move. |

No authentication, no user input, no data storage, no API endpoints. ASVS L1 categories V2-V14 do not apply to this static file restructuring. The threat surface is unchanged from the pre-phase state.
</threat_model>

<verification>
After all tasks complete:
1. `pages/personal.md` exists with `layout: landing`, `title: Personal`, `permalink: /personal`
2. `index.html` does not exist at repository root
3. `pages/about.md` has `href="/personal"` on the image link (no `href="/"`)
4. `pages/sets.md` has `href="/personal"` on the home link (no `href="/"`)
5. `bundle exec jekyll build` completes with exit code 0 and no errors
6. `_site/personal/index.html` exists in build output (Jekyll generated the page)
7. `_site/index.html` does NOT exist in build output (root is empty)
8. All other pages (`/about`, `/photos`, `/notes`, `/notes-password`, `/sets`, `/contact`) still have their `_site/` output directories intact
</verification>

<success_criteria>
- Landing page loads at `/personal` with all JavaScript interactions working (magnetic links, logo rotation, tooltip, footer flash)
- Root `/` returns 404
- About page back-link navigates to `/personal`
- Sets page back-link navigates to `/personal`
- All existing pages load at their current URLs without errors
- Jekyll build produces zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-route-restructure/01-01-SUMMARY.md`
</output>
