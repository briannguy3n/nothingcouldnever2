---
phase: 02-new-pages
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: []
autonomous: false
requirements: [CHSR-01, CHSR-02, CHSR-03, CHSR-04, LINK-01, LINK-02, LINK-03, LINK-04, LINK-05]
must_haves:
  truths:
    - "Jekyll builds without errors"
    - "/ renders the dark chooser with n0thing and other stuff links"
    - "/links renders the dark linktree with profile header and categorized cards"
    - "Navigation between chooser and linktree works"
    - "Existing pages (/personal, /about, /photos) are unaffected"
  artifacts: []
  key_links:
    - from: "/"
      to: "/links"
      via: "chooser primary link click"
      pattern: "n0thing"
    - from: "/"
      to: "/personal"
      via: "chooser secondary link click"
      pattern: "other stuff"
---

<objective>
Verify the complete Phase 2 build — Jekyll compiles cleanly, both new pages render correctly with dark aesthetic and all UI-SPEC values, navigation works, and existing site pages are unaffected.

Purpose: Catch build errors, SCSS bleed, broken links, or visual regressions before shipping.
Output: Verified working site with chooser at / and linktree at /links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-new-pages/02-CONTEXT.md
@.planning/phases/02-new-pages/02-UI-SPEC.md
@.planning/phases/02-new-pages/02-01-SUMMARY.md
@.planning/phases/02-new-pages/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Jekyll and verify output files</name>
  <files></files>
  <read_first>
    - _config.yml (build configuration, permalink style)
    - index.md (confirm front matter is correct before build)
    - pages/links.md (confirm front matter)
    - _sass/_-sections-dir.scss (confirm both imports registered)
  </read_first>
  <action>
Run Jekyll build and verify all expected output files exist in _site/.

1. Run `bundle exec jekyll build` from project root. Expect exit code 0 with zero errors.
   - SCSS deprecation warnings from sass-embedded are pre-existing and acceptable (confirmed in Phase 1 summary).
   - The ONLY acceptable warnings are SCSS deprecation warnings. Any "Conflict" warnings about duplicate destinations = build problem.

2. After successful build, verify these output files exist:
   - `_site/index.html` — chooser page rendered from index.md + chooser layout
   - `_site/personal.html` — existing landing page (Phase 1)
   - `_site/links.html` — linktree page rendered from pages/links.md + linktree layout (note: permalink /:title produces .html not directory)

3. Verify chooser output contains expected elements:
   - `grep 'chooser-page' _site/index.html` — body class present
   - `grep 'href="/links"' _site/index.html` — n0thing link present
   - `grep 'href="/personal"' _site/index.html` — other stuff link present

4. Verify linktree output contains expected elements:
   - `grep 'linktree-page' _site/links.html` — body class present
   - `grep 'linktree-card' _site/links.html` — card elements rendered
   - `grep 'noopener noreferrer' _site/links.html` — security attribute present
   - `grep 'linktree-thumb--releases' _site/links.html` — color_key classes rendered

5. Verify existing pages unaffected:
   - `_site/personal.html` exists (Phase 1 landing page)
   - `_site/about.html` exists
   - `_site/photos.html` exists

6. Verify main.css includes new styles:
   - `grep 'chooser-page' _site/assets/css/main.css` — chooser styles compiled
   - `grep 'linktree-page' _site/assets/css/main.css` — linktree styles compiled
  </action>
  <verify>
    <automated>cd /Users/brian/Documents/GitHub/nothingcouldnever2 && bundle exec jekyll build 2>&1 | tail -5 && test -f _site/index.html && test -f _site/links.html && test -f _site/personal.html && grep -q "chooser-page" _site/index.html && grep -q "linktree-card" _site/links.html && grep -q "chooser-page" _site/assets/css/main.css && grep -q "linktree-page" _site/assets/css/main.css && echo "BUILD PASS" || echo "BUILD FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `bundle exec jekyll build` exits with code 0
    - No "Conflict" warnings in build output (duplicate destination warnings)
    - _site/index.html exists and contains "chooser-page", 'href="/links"', 'href="/personal"'
    - _site/links.html exists and contains "linktree-page", "linktree-card", "noopener noreferrer", "linktree-thumb--releases"
    - _site/personal.html exists (existing landing page preserved)
    - _site/about.html exists (existing page preserved)
    - _site/assets/css/main.css contains "chooser-page" and "linktree-page" (SCSS compiled)
  </acceptance_criteria>
  <done>Jekyll builds cleanly; chooser and linktree HTML output correct; existing pages preserved; compiled CSS includes both new partials</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of both new pages</name>
  <files></files>
  <action>
Present both new pages to user for visual verification. Start Jekyll dev server with `bundle exec jekyll serve` and provide verification checklist. This is a human-verify checkpoint — Claude starts the server, user checks in browser.
  </action>
  <what-built>Two new pages: chooser splash at / (dark, centered "n0thing" + "other stuff" links) and linktree at /links (dark card-based link hub with profile header, categorized cards with colored thumbnails). Both pages are dark (#000 background) with the full UI-SPEC styling applied. All existing site pages remain intact.</what-built>
  <how-to-verify>
    1. Start the Jekyll dev server: `bundle exec jekyll serve`
    2. Open http://localhost:4000/ in your browser
       - Expect: dark black page, "n0thing" in large white text centered vertically, "other stuff" smaller and grey below it
       - Hover over "n0thing" — should fade to grey (#aaaaaa)
       - Hover over "other stuff" — should brighten slightly (#cccccc)
       - No other elements on the page (no logo, nav, or footer)
    3. Click "n0thing" — should navigate to http://localhost:4000/links
       - Expect: dark page with circular avatar placeholder and "n0thing" heading at top
       - Below: 4 category sections (Releases, Sets and Recordings, Tour / Shows, Video)
       - Each category has cards with a colored left block (green, blue, purple, red) and placeholder title text
       - Hover a card — should lift slightly and show faint white border
    4. Go back to http://localhost:4000/ and click "other stuff" — should navigate to /personal (the existing landing page)
    5. Verify existing pages still work:
       - http://localhost:4000/personal — existing landing with magnetic links
       - http://localhost:4000/about — about page loads
       - http://localhost:4000/photos — gallery loads
    6. Check mobile: resize browser to ~375px width
       - Chooser: "n0thing" text should be smaller (3rem)
       - Linktree: cards should be slightly shorter (56px)
  </how-to-verify>
  <verify>User visually confirms both pages match UI-SPEC</verify>
  <done>User approves visual appearance and navigation of both new pages</done>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Verification-only plan — no new code, no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | Verification plan introduces no new threats |
</threat_model>

<verification>
- Jekyll build succeeds with zero errors
- Both new pages render in browser with correct dark aesthetic
- Navigation between pages works (/ -> /links, / -> /personal)
- Card hover effects work (lift + border)
- Existing site pages unaffected
- Mobile responsive behavior correct
</verification>

<success_criteria>
- Jekyll build passes
- User approves visual appearance of both pages
- All 9 requirements (CHSR-01 through CHSR-04, LINK-01 through LINK-05) verified working
</success_criteria>

<output>
After completion, create `.planning/phases/02-new-pages/02-03-SUMMARY.md`
</output>
